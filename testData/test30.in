#Calling Function
def merge(str1, str2):
    return str1 + str2

def superMerge(str1, str2):
    return merge(merge(merge(str1, str2), str1), str2)

hello = "Hello"
world = "World"
ans = superMerge(hello, world)
print(ans)
