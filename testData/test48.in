#Factorization
def factor(x) :
    i = 2
    while True:
        if i * i > x:
            break
        while x % i == 0:
            x //= i
            print(i)
        i = i + 1
    if x > 1:
        print(x)

factor(10007)
factor(124356)
factor(512854711)
